#pragma once
#include "vec2.h"
#include "vec3.h"
#include "vec4.h"

using namespace math;

/*template <typename T, size_t A>
__forceinline vec4<T,A>::vec4(const vec2<T,A> &a, const vec2<T,A> &b)
{
	return vec4(a.x,a.y,b.x,b.y);
};*/